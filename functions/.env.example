# Used by Cloud Functions (local emulator loads via dotenv). In production set the same
# variable with `firebase functions:config:set` or Google Cloud env / Secret Manager.
ENCRYPTION_KEY=

# Optional — enables TMDB title/overview append when playlist has enrichEnabled.
TMDB_API_KEY=
